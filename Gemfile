# After any changes to the Gemfile, execute: bundle update
source "https://rubygems.org"

# Lock Jekyll + plugins to GitHub Pages’ supported versions
gem "github-pages", group: :jekyll_plugins

# Agency theme as a gem (NOT remote_theme)
gem "jekyll-agency"

# Windows-only file watcher (leave commented unless on Windows)
# gem "wdm", ">= 0.1.0" if Gem.win_platform?
